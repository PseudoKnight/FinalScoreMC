// Sets the equipment for the start of the game
proc _shard_equip(@player) {
    noop();
}

// Binds events specific to this mode
proc _shard_bind(){
    noop();
}

// Unbinds events specific to this mode
proc _shard_unbind(){
    noop();
}
