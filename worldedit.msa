*:'//up' [$] = call_alias('/up '.$);
*:/up [$amount=0] = >>>
    @loc = _relative(ploc(), 'up', integer($amount));
    if(@loc['y'] > 255 || sk_can_build(@loc)) {
        run('/up '.$amount);
    } else {
        die(color('red').'You are not allowed to build there.');
    }
<<<
